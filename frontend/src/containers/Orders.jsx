import React, { Fragment, useEffect } from 'react';
import { fetchLineFoods } from '../apis/line_foods';

export const Orders = () => {
  useEffect
  return (
    <Fragment>
      注文画面
    </Fragment>
  )
}